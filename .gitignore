# Progress data generated by FOREMAN in local mode.
# In global mode this directory lives outside the repo entirely.
# Root-anchored: only ignores /progress/ at the repository root, not templates/progress/.
/progress/

# macOS
.DS_Store

# Editor directories
.idea/
.vscode/
