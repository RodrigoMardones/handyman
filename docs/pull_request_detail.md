## Revisores

- RodrigoMardones

## Cambios

- Agrega el ciclo de vida de sprints al harness con `handyman/scripts/sprint.py` (nuevo): `open <id>` estampa la etiqueta de periodo (`sprint: "2026-SP1"`) en las features `pending`/`in_progress` sin etiqueta y registra `current_sprint` en `harness.config.json` (con espejo en el bloque `config` de `feature_list.json`); `close` deriva el documento formal del periodo en `docs/sprints/sprint.<id>.md` desde los artefactos existentes (`feature_list.json`, encabezados fechados de `progress/history.md`, frontmatter de `backlog/`), archiva las features `done` del sprint en `archive/feature_archive.json`, quita la etiqueta a las carry-over y limpia `current_sprint`, con previsualización vía `--dry-run`; `status` reporta el sprint abierto. Nueva plantilla `handyman/assets/sprint.template.md` (secciones derivadas + Logros/Lecciones manuales) y nueva suite `tests/test_sprint.sh` (8 casos), cableada como 12ª suite en `tests/run_tests.sh`.
- Sella el contrato de sprint schema-first: clave opcional `sprint` (patrón `^\d{4}-SP\d+$`) en la definición de feature de `handyman/assets/schemas/feature_list.schema.json` y `current_sprint` (string nullable) en las definiciones de config de ambos schemas (`feature_list` y `harness.config`); centinela `"current_sprint": null` en las tres plantillas de assets. La etiqueta es partición, no cronología: el contrato sigue siendo una máquina de 4 estados sin fechas. Nuevo `test_sprint_config()` en `tests/test_docs.py` (12 checks: acepta `2026-SP1`, rechaza etiquetas malformadas, `additionalProperties:false` intacto).
- Agrega procedencia de rama a la sesión: `handyman/scripts/feature.py start` registra la rama git en `progress/current.md` (línea `- **Branch:**`, con placeholder fuera de un repo git; usa `git symbolic-ref` que funciona en repos sin commits) y `done` la arrastra a la entrada rica de `progress/history.md`; `handyman/scripts/validate_harness.py` gana el advisory no bloqueante `check_branch_advisory` que imprime un NOTE cuando la sesión de `current.md` pertenece a otra rama (apunta a retomar/bloquear/worktree). La rama NO entra al contrato de feature. Además `feature.py add` ahora respeta el máximo id del archivo de archivo (`_archived_max_id`) para nunca reusar ids archivados tras un cierre de sprint. Tests F19/F20/F21 en `tests/test_feature.sh` y T17 en `tests/test_init.sh`.
- Separa la documentación del workspace en dos espacios de periodo: `handyman/scripts/scaffold.sh` crea `docs/current/` (borradores sin revisar del sprint abierto) y `docs/sprints/` (documentos de periodos cerrados); `handyman/scripts/index_md.py` lista ambos como wikilinks en el MOC. Tests T18 en `tests/test_init.sh` e I6 en `tests/test_index.sh`.
- Documenta el protocolo de sprint en las referencias: fila de etapa 7 (`sprint.py close` → documento de sprint) y nueva sección `## Sprint Protocol` (open → trabajo → close → pase manual) en `handyman/references/workflow.md`, más el párrafo de multi-rama/worktrees en Startup (el workspace es uno por checkout); `handyman/references/anatomy.md` suma las filas de soporte (sprint.py, docs/current/, docs/sprints/) y extiende el Feature List Contract con la etiqueta `sprint` ("partition, not chronology") y `current_sprint` en el espejo de config; `handyman/references/checklists.md` gana el `## Sprint-Close Checklist` (6 items). `SKILL.md` queda intacto por presupuesto (998/1000 palabras).
- Agrega el documento de investigación `docs/analisis-sprints-cierre-periodo.md` (feature 92), que sustenta el plan A-E anterior con evidencia del repositorio (workspace singleton por checkout, invariante single-in_progress global, crecimiento sin cierre de history/backlog/feature_list) y literatura de las skills `handyman`, `skill-creator` y `ponytail`.

## Tarea o asunto asociado

- feat/documentation-update-and-sprint-clousure

## Evidencia del cambio

- `bash tests/run_tests.sh`: ALL SUITES PASSED (12 suites: docs 169, init 16, update 12, feature 21, backlog 7, index 6, upgrade 10, tools-discovery 16, evals 7, preflight 8, metrics 6, sprint 8).
- `./init.sh`: exit 0 (todas las fases y advisories pasan).
- `shellcheck -S warning` sobre los shell scripts tocados: limpio.
- Dogfood en vivo: sprint `2026-SP1` abierto y cerrado sobre este mismo repositorio — generó `.handyman/docs/sprints/sprint.2026-SP1.md` (6 features done, verdicts 6-0, cobertura 6/6, 4 carry-over), archivó las features del periodo y las 86 legacy (`pre-2026-SP1`) dejando `feature_list.json` con solo el trabajo vivo; revisión batch independiente por subagente reviewer: APPROVED.
