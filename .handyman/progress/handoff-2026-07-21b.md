---
type: Doc
---

# Handoff · 2026-07-21b · MCP server shipped, memory/ layout live, PR #28 open

**Branch:** `feat/rework-tools` · **PR:** [#28](https://github.com/RodrigoMardones/handyman/pull/28) (open, base `main`)
**Gate:** `./init.sh` exit 0 (certified by `feature done` of 72 and 73)
**Backlog:** 69–73 `done` (69–71 committed at `8220e36`, 72 at `fdb9ba4`, 73 at `0b571b9`) · queue drained
**Open period:** `feat-rework-tools` — run `node handyman/dist/sprint.js close` **at merge time** (first real run of checkpoint C6; archives 69–73)
**Previous:** [handoff-2026-07-21.md](handoff-2026-07-21.md) (note: handoffs now live in `progress/`, not `docs/current/`)

## 1. What shipped this session

- **Feature 72 `handyman_mcp_server`** → [impl](../backlog/impl_handyman_mcp_server.md).
  `handyman/src/mcp.ts`: thin stdio wrapper over the same dist CLIs — 6 tools
  (`feature_close` refuses on a red verifier via subprocess, no force flag) +
  `current`/`docs` resources, multi-repo via `$HANDYMAN_ROOT/registry.json`.
  Registered in `discovery.mcp` + `.vscode/mcp.json`. Suite `tests/test_mcp.js`
  (8/8, real JSON-RPC) wired into `run_tests.sh`. SKILL.md gained
  "Mechanics: MCP First" and `references/mcp.md` (English); SKILL trimmed to
  ≤1000 words (the docs suite enforces it — it caught the first close).
- **Feature 73 `workspace_memory_layout`** → [impl](../backlog/impl_workspace_memory_layout.md).
  Workspace knowledge dir `docs/` → `memory/` through a single
  `resolveDocsDir(workspace)` in toolbox-core (memory/ preferred, docs/
  legacy fallback, memory/ for fresh workspaces). Handoffs merged from
  `docs/current/` into `progress/` with links rewritten. Surfaces kept their
  names on purpose (MCP `.../docs/*` URIs, observer `docs:` tokens) — zero
  `apps/web` changes. Scaffold/templates/skill emit the new layout.
- Both features' impl/review reports were written with the server's own
  `report_write` (dogfooding).

## 2. Read before touching anything

1. **The global skill was NOT reinstalled.** Repo `handyman/SKILL.md` is ahead
   of `~/.claude/skills/handyman/`; validate `tests/lab_skill_install.sh`
   before reinstalling (standing warning).
2. **Registered harnesses (cmcet-back, phily-app) still use `docs/`** — that
   is fine forever via the fallback; do not migrate them casually.
3. **Graphify manifest is stale twice over**: the 21 docs from the previous
   session plus every path moved by the `memory/` rename. Next
   `/graphify --update` re-detects both; if it reports thin cache hits,
   delete those `graphify-out/cache/` entries to force real extraction.
4. **History stubs that say `docs/sprints/...` are historical text** — written
   before the rename, intentionally untouched.

## 3. Next steps, in order

1. Merge PR #28; on merge run `sprint.js close` (checkpoint C6 debut —
   archives 69–73 under `feat-rework-tools`).
2. `/graphify --update` (see §2.3).
3. Named futures, still not built: sprint → period cosmetic rename in
   code/templates; parallel multi-branch sessions (membership derived from
   branch provenance at `close`); npm publish path (features 64–66 registered).
