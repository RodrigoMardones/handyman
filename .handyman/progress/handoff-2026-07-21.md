---
type: Doc
---

# Handoff · 2026-07-21 · rework de capas ejecutado: rama como período, graphify vivo, señal de discovery

**Rama:** `feat/rework-tools` · **PR:** no existe todavía — **NADA está commiteado** (395 paths en el working tree)
**Gate:** `./init.sh` exit 0 (certificado por el `feature done` de la 71)
**Backlog:** 69–71 `done` · **72 `pending`** (`handyman_mcp_server`, max id = 72; `ready` la lista porque sus depends 69–71 cerraron)
**Período abierto:** `feat-rework-tools` (sí: el label es la rama — ver §1)
**Anterior:** [handoff-2026-07-19b.md](handoff-2026-07-19b.md) · **Investigación base:** [explore_reorganizacion_capas.md](../backlog/explore_reorganizacion_capas.md)

---

## 0. Lee esto antes de tocar nada

1. **Todo el rework vive sin commit en `feat/rework-tools`.** El primer acto de la
   próxima sesión no es codear: es revisar el diff, commitear y publicar el PR
   (`pull-request-publish`). El grueso de los 395 paths son las 318 `D` del
   backlog movido a `archive/backlog/` — es un `git mv` lógico, no una pérdida.
2. **El protocolo de período cambió.** El sprint calendario murió: la rama es la
   unidad. Al abrir rama: `node handyman/dist/sprint.js open <rama-slug>`; al
   mergear: `sprint.js close` (checkpoint C6 nuevo en CHECKPOINTS.md). El merge
   de esta rama será el **estreno real** de ese cierre.
3. **La entrada de history ahora es compacta** (Branch/Tools/Evidence/
   Verification, 5 líneas). No la "arregles" de vuelta al formato de 8 campos:
   ese formato se auto-degradaba a `Plan: ...` y se retiró a propósito
   (emisor en `feature.ts`, plantilla y pins de tests alineados).
4. **Graphify tiene un hueco conocido y auto-corregible:** 21 docs (sprints,
   verification.md, AGENTS/CHECKPOINTS, index, current, feature-request)
   quedaron **fuera del manifest a propósito** — su chunk de extracción murió
   por límite de sesión. El próximo `/graphify --update` los re-detecta. Ojo:
   si el update los reporta como *cache hit* con contenido flaco (stubs), borra
   sus entradas en `graphify-out/cache/` para forzar extracción real.
5. **No reinstales la skill global** sin verificar `tests/lab_skill_install.sh`
   (advertencia heredada del handoff anterior, sigue vigente hasta validar el
   paquete npm contra esta rama).

## 1. Qué pasó en esta sesión

Ejecución completa del plan de [explore_reorganizacion_capas.md](../backlog/explore_reorganizacion_capas.md),
una feature formal por fase, cada una cerrada con `feature done` y verifier verde:

- **F0 = 69 `period_close_branch_unit`** → [impl](../backlog/impl_period_close_branch_unit.md).
  Cierre pendiente de SP6: 39 done archivadas (`feature_list.json` 73 KB → 5.7 KB),
  history compactado, backlog 325 → 7 explores vivos (318 a `archive/backlog/`).
  Producto: `SPRINT_ID` acepta slugs de rama; `add`/`start` estampan el período
  abierto (el hueco que varó 19 done en SP6); schemas ×4 alineados;
  `core/period.ts` nuevo comparte `readCurrentSprint`. Plantilla única de
  petición (`request.template.md` eliminado — su contenido estaba implementado).
  Global limpio (`registry.json` intacto). Suites: sprint 13/13, feature 43/43.
- **F1 = 70 `graphify_freshness_gate`** → [impl](../backlog/impl_graphify_freshness_gate.md).
  Preflight ganó el bloque `context` (NOTE grafo viejo / OK fresco, advisory
  puro, nunca gatea strict). Grafo refrescado de verdad: 2185 nodos / 4268
  aristas / 134 comunidades, `.handyman/docs` indexada por primera vez, 347
  fantasmas de `analisis-*` podados, `cost.json` iniciado (423K tokens input).
  Regla única de análisis en conventions.md; 20 análisis cumplidos en
  `docs/archive/`; `.next` excluido del link-checker de test_docs.
- **F2 = 71 `discovery_declared_paths`** → [impl](../backlog/impl_discovery_declared_paths.md).
  **Renegociada** (aceptación reescrita vía `feature acceptance` antes de
  implementar): persistir `{name,path}` contradecía el diseño documentado del
  propio `tools_discovery` y `models` default+overrides era ceremonia sin
  consumidor. Lo real: NOTEs de no-declarada solo para skills **locales** del
  repo — de ~30 de ruido global a 13 accionables. Suite 17/17.

## 2. Próximos pasos, en orden

1. **Revisar diff + commit + PR** con `pull-request-publish`; en el merge,
   correr `sprint.js close` (estrena C6 y archiva 69–71 bajo el período
   `feat-rework-tools`).
2. **Feature 72 `handyman_mcp_server`** (F3): wrapper MCP delgado sobre
   `handyman/src/core` — contrato en código (`feature_close` gated por
   verifier), hub multi-repo vía `$HOME/HANDYMAN/registry.json`, portabilidad.
   Construir con `mcp-builder`. Superficie mínima en la descripción de la
   feature y en el explore.
3. **Próximo `/graphify --update`**: re-extrae los 21 docs del punto §0.4.
4. **Futuros nombrados, no construidos:** rename cosmético sprint → period en
   código/plantillas; ¿`docs/current/` (handoffs) se fusiona en `progress/`?;
   rename `docs/` → `memory/`; sesiones paralelas multi-rama (deriva de
   membresía por provenance de rama en `close`).
