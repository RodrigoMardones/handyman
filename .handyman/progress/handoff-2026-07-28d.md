---
type: Doc
---

# Handoff · 2026-07-28d · Spike Mastra COMPLETO (fases 0–4): evals en CI verdes con exit code, skill nativa espejo, ledger fiel por traceId, ADR propuesto (adopción + sunset Flue) a ratificar

**Branch:** `feat/flue-spike` · **Estado de commits:** el usuario commiteó a media sesión (`5898a13`, 13:10) — incluye Flue + fases 0–2 + primeros archivos de fase 3. Lo posterior queda sin commitear (§5).
**Contexto:** cuarta sesión del día. Continuación de [handoff-2026-07-28c.md](handoff-2026-07-28c.md). Fase 4 del spike (evals + skill + ADR) más la deuda de tokens. Fuera del ciclo de features del harness (evidencia = impl report).
**Previous:** [handoff-2026-07-28c.md](handoff-2026-07-28c.md)

## 1. Qué se entregó

- **`docs/adr-mastra-adopcion.md`** — ADR: adoptar Mastra como runtime del harness (dos topologías, una definición de roles) + **sunset de `agents/flue-handyman/`** (deprecated, sin features; eliminación al siguiente sprint si sigue verde) + retiro de la vista `/agent` (SDK Flue). **Estado: propuesto — a ratificar por el operador.**
- **Evals en CI (`run-evals.ts`, script `test:eval`)**: `runEvals` con gates (`checks.toolOrder`, `checks.noToolErrors`) + scorer propio `protocol-trajectory-order` (zero-LLM, threshold 1.0). Dos casos reales: verde → `passed`+`done`; verifier rojo → `passed`+`in_progress`. **EXIT_CODE=0**. Scores persistidos en `score_events`.
- **Skill handyman nativa espejo (`src/agents/handyman-skill.ts` + `run-skill.ts`)**: `handyman/SKILL.md` canónico como skill agent-level por path (cero duplicación), sin role instructions. Ciclo completo verificado: `skill → add → start → log → report_write ×2 → backlog_review → close` → `done` (74.8s), `validate_harness: OK`. Recuperó además una feature atascada de una corrida previa cerrándola por el gate del verifier.
- **Ledger fiel (`src/ports/usage-aggregate.ts`)**: `scope:"run"` agregado por **traceId** = in 89 782 / out 2 356 vs leader-only 49 614 / 774 (1.8×). threadId no sirve (delegaciones = threads frescos).
- **Disciplina del leader** (instrucciones): close solo SYNC, una delegación por rol, nunca `task_result`/escrituras de subagente. Drivers: filtro de `--` en argv (bug pnpm en los 3).
- **Validación:** evals 2/2 PASS con exit 0 · skill ciclo completo · ledger 1.8× · `vitest` 23/23 · `tsc` limpio.

## 2. Estado operativo (cómo retomar)

```bash
node handyman/dist/mcp.js --http --port 8177          # MCP (vivo en esta sesión, task bash-9ycv5f2g)
cd agents/mastra-handyman && set -a && . ../../.env && set +a
HANDYMAN_PROJECT_ROOT=/tmp/hm-mastra-eval pnpm test:eval          # gate CI (exit 0/1)
HANDYMAN_PROJECT_ROOT=/tmp/hm-mastra-skill pnpm run-skill -- <f>  # espejo skill
HANDYMAN_PROJECT_ROOT=<scratch> pnpm run-feature -- <f>           # supervisor (ledger scope=run)
HANDYMAN_PROJECT_ROOT=<scratch> pnpm run-workflow -- start <f>    # workflow durable
```

- **Scratchs vivos en /tmp:** `hm-mastra-eval` (8 features eval, todas done) · `hm-mastra-skill` (3 done) · `hm-mastra-ledger` (2 done, tokens.jsonl con líneas leader+run) · más los `hm-mastra-wf-*` de fase 3 y los de fases 0–2. Data dirs: `hm-mastra-data-{skill,ledger,wf-*}` + `./data` del paquete (traces de evals).
- **Ratificación del ADR:** al ratificarse, marcar `agents/flue-handyman/README.md` como deprecated y agendar la eliminación del paquete + retiro de `apps/web/app/{agent,api/agent}`.

## 3. Gotchas nuevos (sumar a los 14 acumulados — numeración del README)

15. **`runEvals` forma config (AgentScorerConfig) no admite gates ni thresholds**; la forma plana sí, pero alimenta mensajes crudos a los scorers de trayectoria (ver §3.16). Diseño final: forma plana (gates + threshold) + scorer propio.
16. **Trayectoria por trace inútil para agents** (1.53.0): top level = `llm: 'glm-5.2'` (MCP calls anidan 3 niveles abajo). El prebuilt `trajectory-accuracy` nunca matchea; scorer propio sobre `extractTrajectory(output)`.
17. **El leader diverge bajo verifier rojo** (3 formas en 3 corridas): async close + `task_result` inventado; doble delegación; agotamiento de steps. Mitigado con prompt; el enforcement real es el verifier/workflow, no el modelo.
18. **`noToolErrors` no ve errores de envelope MCP** (`isError` = tool result con texto, no tool-error SDK).
19. **Skill nativa cara en tokens**: 376k primera corrida → 129k con disciplina. Supervisor ≈ 90k; workflow ≈ la mitad (más barato por feature).
20. **Scores sin registrar advierten**: `saveScoresToStorage` busca el scorer por id en la instancia — registrar los ids en `new Mastra({ scorers })` (persistencia limpia en `score_events`).

## 4. Loose ends / próximos pasos

- **Ratificar el ADR** (operador): adopción + sunset Flue + retiro vista `/agent`.
- **Incidente de deriva #2 (ya limpiado):** `report_write` del caso `eval_red` escrito en el backlog del monorepo (detectado por git status; limpieza quirúrgica verificada — `feature_list.json`, `current.md`, `run/` intactos). **El pinning de proyecto a nivel MCP sube de prioridad: 2 incidentes, la única vía de contaminación real observada.**
- **Deuda estructural:** lectura de backlog por MCP; issue upstream restart+`.map()`.
- **Aditivos post-ADR:** semantic recall / Observational Memory (capa conversacional, nunca negocio); Studio como panel; Workspace+filesystem para skill mirror en proyectos reales.
- **Commit + PR:** decisión del usuario (árbol en §5).

## 5. Estado del árbol (git status resumen)

**Sin commitear (todo posterior a `5898a13`):**
- Nuevos: `agents/mastra-handyman/{run-workflow.ts, run-evals.ts, run-skill.ts, src/workflows/, src/evals/, src/agents/handyman-skill.ts, src/ports/usage-aggregate.ts(+test)}` · `.handyman/backlog/impl_mastra_spike_phase_{3,4}.md` · `docs/adr-mastra-adopcion.md` · `.handyman/progress/handoff-2026-07-28{c,d}.md`.
- Modificados: `agents/mastra-handyman/{README.md, package.json, src/app.ts, src/mastra/index.ts, src/agents/handyman-leader.ts, src/ports/tokens-ledger.ts}` · `docs/spike-mastra-harness.md` (header) · `pnpm-lock.yaml`.
- `agents/mastra-handyman/{package.json,tsconfig.json}` con zod+scripts quedaron dentro de `5898a13`.
**Limpieza hecha:** probes throwaway de fase 3 y 4 eliminados tras extraer hallazgos.
