---
type: Doc
---

# Handoff · 2026-07-28e · Mandato del operador EJECUTADO: Flue eliminado (+vista /agent), agentes con sistema real (Workspace+web+git/GitHub), deuda backlog resuelta sin capa nueva, skill mirror relegada al workflow

**Branch:** `feat/flue-spike` · **Estado de commits:** todo sin commitear (incluye lo del handoff-d); commit + PR = decisión del usuario.
**Contexto:** quinta sesión del día. Continuación de [handoff-2026-07-28d.md](handoff-2026-07-28d.md). El operador ratificó el ADR y dictó 4 decisiones; ejecución completa con evidencia en `.handyman/backlog/impl_mastra_adopcion_mandato.md`.
**Previous:** [handoff-2026-07-28d.md](handoff-2026-07-28d.md)

## 1. Qué se entregó (por decisión)

1. **Sunset Flue ejecutado** (sin ventana): `agents/flue-handyman/` eliminado; `apps/web/app/{agent,api/agent}` + `components/AgentLive.tsx` retirados; suites `test_flue_agents.sh`/`test_web_agent.sh` eliminadas; scripts raíz `agents:*` repuntados a mastra; `allowBuilds` @flue limpiado (−203 paquetes); ADR Mastra **ratificado**, ADR Flue histórico; `AGENTS.md` + `memory/architecture.md` actualizados. Suite completa verde.
2. **Sistema real para los agentes** (docs Mastra 1.53.0, latest): `src/ports/workspace.ts` — Workspace por rol (implementer/skill: FS escritura + sandbox; leader/reviewer: FS read-only enforced por `WorkspaceReadOnlyError`); `src/ports/web-tools.ts` — `web_search` (DuckDuckGo Lite, sin key) + `web_fetch` (HTML→texto capado); git = CLI vía sandbox; **GitHub = MCP oficial** en el mismo `MCPClient` cuando hay `GITHUB_TOKEN`/`GH_TOKEN` (leader-only por construcción). Barrel `src/mastra/index.ts` extendido (anti-volatilidad intacta).
3. **Deuda estructural backlog: RESUELTA sin capa nueva.** El problema real: el backlog no era legible EN ABSOLUTO (MCP solo escribe/estampa). Respuesta: NO a tool MCP de lectura (el FS read-only del reviewer lo cubre), NO a informes en la DB de Mastra (`feature.js done` lee el veredicto DE DISCO — rompería el gate — y viola la regla §3 del ADR). Instrucciones del reviewer actualizadas: lee `impl_<f>.md` del disco antes de juzgar.
4. **Skill mirror relegada:** workflow durable = camino por defecto del ciclo (más barato, determinista, HITL); skill mirror queda como validación de formato/adopción, ahora con workspace+web (el aditivo post-ADR ejecutado). Documentado en README §13.

## 2. Estado operativo (cómo retomar)

Sin cambios en los comandos (handoff-d §2). Nuevo: `GITHUB_TOKEN`/`GH_TOKEN` en env activa las `github_*` tools del leader; sin token, `gh` CLI autenticado funciona en el sandbox del implementer.

- **Verificación de la sesión:** `tsc` limpio (web + mastra) · `vitest` 23/23 · `next build` verde sin rutas `/agent` · `run_tests.sh` ALL SUITES PASSED · sonda live (eliminada): búsqueda real 8 resultados, fetch OK, read-only enforced, `mastra_workspace_write_file` ejecutada en corrida GLM real.

## 3. Gotchas nuevos (numeración del README mastra)

15. **Las workspace tools NO aparecen en `agent.listTools()`** — se inyectan por run (`createWorkspaceTools` sobre `getWorkspace()`); el wiring se verifica por ejecución, no por inspección.
16. **`LocalFilesystem.readOnly` se enforcea en código** (`WorkspaceReadOnlyError`); `listFiles` no está en la interfaz `WorkspaceFilesystem` programática (solo en las tools del agente).

Gotcha menor corregido: la tabla de scores es `mastra_scorers`, no `score_events` (README arreglado).

## 4. Loose ends / próximos pasos

- **Pinning de proyecto a nivel MCP** — prioridad alta confirmada (2 incidentes de deriva; única vía de contaminación real).
- **Issue upstream restart+`.map()`** — sigue sin filear.
- `web_search` = scraper de DuckDuckGo Lite sin SLA; upgrade path: `TAVILY_API_KEY` + `@mastra/tavily`.
- Aditivos conversacionales pendientes: semantic recall / Observational Memory, Studio como panel.
- **Commit + PR:** decisión del usuario (árbol en §5).

## 5. Estado del árbol (git status resumen)

**Eliminados:** `agents/flue-handyman/` (todo) · `apps/web/app/agent/` · `apps/web/app/api/agent/` · `apps/web/components/AgentLive.tsx` · `tests/test_flue_agents.sh` · `tests/test_web_agent.sh`.
**Nuevos (esta sesión):** `agents/mastra-handyman/src/ports/workspace.ts` · `agents/mastra-handyman/src/ports/web-tools.ts` · `.handyman/backlog/impl_mastra_adopcion_mandato.md` · este handoff.
**Modificados (esta sesión):** `package.json` (scripts) · `pnpm-workspace.yaml` · `pnpm-lock.yaml` · `tests/run_tests.sh` · `AGENTS.md` · `docs/adr-mastra-adopcion.md` (ratificado) · `docs/adr-flue-harness-architecture.md` (histórico) · `.handyman/memory/architecture.md` (capa Mastra) · `agents/mastra-handyman/{README.md, src/mastra/index.ts, src/agents/handyman-leader.ts, src/agents/handyman-skill.ts, src/workflows/feature-cycle.ts}` · `.handyman/index.md` (regenerado).
Más todo lo del handoff-d §5 (sin commitear).
