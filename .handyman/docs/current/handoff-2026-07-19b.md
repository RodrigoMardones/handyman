---
type: Doc
---

# Handoff · 2026-07-19b · decisiones OSS ejecutadas + plan de investigación y review

**Rama:** `feat/llm-toolbox-tasks` · **PR:** [#26 abierto hacia `main`](https://github.com/RodrigoMardones/handyman/pull/26), cuerpo y título actualizados
**Gate:** `./init.sh` exit 0 · `run_tests.sh` ALL SUITES PASSED (36 suites, 692 casos)
**Backlog:** 61-63 `done` · **64-66 `pending`** (max id = 66) · `ready` lista 64 y 66
**Anterior:** [handoff-2026-07-19.md](handoff-2026-07-19.md) · **Decisiones de negocio:** tomadas por el humano el 2026-07-19 (ver §1)

---

## 0. Lee esto antes de tocar nada

**El handoff anterior quedó desactualizado en tres afirmaciones.** Decía "no hay PR
abierto" (existe el #26 desde antes y ya está actualizado), contaba 42 NOTEs (eran 72:
se sumaron 30 de discovery por skills instaladas después), y su §3.1 ya está hecho
(feature 61). Regla de la casa, otra vez: **verificá contra el árbol antes de creerle
a un plan, incluido éste.**

**NO reinstales la skill global (`~/.claude/skills/handyman`) hasta que 64-65 aterricen.**
La copia instalada es un snapshot pre-split que funciona por accidente (era autocontenida
y se buildeó in situ). Una reinstalación desde la rama actual entrega un paquete **no
ejecutable**: `npm install` revienta con `workspace:*` y no hay `dist/`. Evidencia
reproducible: `bash tests/lab_skill_install.sh`.

## 1. Qué pasó en esta sesión

Las decisiones de negocio se tomaron con el humano (2026-07-19): **monorepo único + CLI
en npm** · **todo MIT** · **limpieza de contenido ajeno** · **PR con auto-firma declarada**.
Ejecutado:

- **Feature 61** — `validate_harness` acepta la convención legacy de frontmatter
  (`verdict:`/`date:`/`reviewer:` → alias de `status:`/`updated:`/`role:`), espejando
  `feature.ts` (`front.status ?? front.verdict`). NOTEs de frontmatter: **32 → 1**.
  El que queda (`explore_workstation_ui_state_2.md`) es deuda real: le faltan las claves
  en ambas convenciones. Caso T29 en `test_init.sh`; T15 sigue de negativo.
- **Feature 62** — los cuatro `package.json` del workspace declaran MIT (los dos que
  decían Apache-2.0 eran las únicas líneas no-MIT del repo).
- **Feature 63** — untrackeadas 14 skills de terceros sin licencia (quedan
  brand-guidelines, frontend-design, ponytail), `graphify-out/cache/` (303 archivos con
  rutas absolutas locales), `cost.json` y los punteros `.graphify_*`. Nada borrado del
  disco. Nota: find-skills también salió — su upstream `vercel-labs/skills` **no tiene
  licencia** (verificado vía `gh api`; el plan original de copiarle el texto MIT partía
  de una premisa falsa).
- **Features 64-66 registradas, no implementadas** (ver §2).
- **PR #26**: push de la rama, cuerpo regenerado desde `docs/pull_request_detail.md`,
  título nuevo. CI (ShellCheck + Test suite) quedó corriendo.
- **Sonda nueva**: `tests/lab_skill_install.sh` (ver §3). No entra al gate:
  `run_tests.sh` lista sus suites explícitamente.
- **CI del PR estuvo rojo por infraestructura, no por código**: el job Test suite moría
  en "Set up Node.js" — pnpm 11.14 usa el builtin `node:sqlite` y exige Node ≥ 22.13,
  pero `ci.yml` instalaba Node 20. Arreglado en `72bffc6` (Node 22 en el workflow; el
  toolchain sigue soportando ≥ 20). **Confirmá los checks verdes antes de mergear #26.**

## 2. Plan de la próxima sesión: investigación + revisión de features

El orden sale del norte (panel como hub + herramientas compartibles), de las dependencias
reales, y de qué puede hacer cada quien:

**2.0 · Acción humana previa (bloquea 64, nadie más puede):** asegurar el nombre en npm.
`handyman` está tomado (v0.0.1 de 2014, abandonado); `handyman-harness` estaba **libre**
al 2026-07-19; el scope `@rodrigomardones/*` queda disponible al tener el usuario npm.
Decidir y, si es `handyman-harness`, reservarlo (basta un `npm publish` inicial o
crear el placeholder). Sin esto, 64 no puede cerrar su acceptance.

**2.1 · Feature 66 (S, sin dependencias) — README de arranque del panel.** El hueco más
barato con más impacto de visibilidad: `apps/web` y `toolbox-core` son invisibles para
un usuario externo. Los 4 pasos ya están identificados: `pnpm install`,
`pnpm --filter handyman build`, crear `$HANDYMAN_ROOT/registry.json`, keys LLM opcionales.
Verificar en clon limpio, no en el árbol de trabajo.

**2.2 · Investigación para 64 (antes de codear).** Tres preguntas, cada una con su
experimento:

| Pregunta | Cómo responderla |
|---|---|
| ¿Bundlear `toolbox-core` dentro del paquete (esbuild) o publicarlo aparte? | `npm pack --dry-run` de ambas variantes en un branch de laboratorio; el criterio es el tarball autocontenido y quién más consume el core (hoy: solo `apps/web`, que vive en el monorepo → bundlear es lo lazy) |
| ¿Un `bin` único o 12? | Ver §4: la respuesta ya está decidida por el criterio de condensación — **uno**, `handyman <verbo>`; medir que el dispatcher no rompa las suites bash (siguen apuntando a `dist/*.js`) |
| ¿Qué versión? | El snapshot instalado y la rama dicen 2.1.1 pero son estados incompatibles; el publish inicial debería ser **3.0.0** para que el harness pueda distinguirlos (`update_harness` compara semver) |

Insumos que ya existen: auditoría de 6 agentes (2026-07-19) con el detalle de
acoplamientos, y `dist/` tiene un artefacto stale (`toolbox_serve.js` huérfano) que hay
que limpiar antes del `files` whitelist. **El whitelist debe excluir `.env`** —
`handyman/.env` contiene una API key real y un `npm pack` sin `files` se la llevaría.

**2.3 · Feature 64 → 65.** Con la sonda como oráculo: hoy da `1 OK / 2-4 FAIL esperados`;
al aterrizar 64-65 los pasos 2-4 deben voltear a OK y la sonda se actualiza. 65 además
corrige el drift de `references/toolbox.md:157` (`assets/toolbox_panel.js` ya no existe).

**2.4 · Revisión de features.** Las reviews 51-63 quedaron auto-firmadas por decisión
explícita del humano (aceptadas, deuda visible en el gate). Para el lote 64-66, si se
quiere review independiente, el mecanismo ya existe: otra sesión/actor +
`backlog.js review <f> --status <s> --force` re-emite conservando el cuerpo.
Decidirlo **antes** de implementar 64, no después.

**Candidatos nombrados, no registrados** (S cada uno, tomarlos solo si sobran ganas):
- Los **30 NOTEs de discovery** ("installed but not declared") son el nuevo 76%-ruido:
  o se declaran las skills en `harness.config.json` o el check aprende a resumir
  ("30 instaladas sin declarar" en una línea). Mismo patrón que la feature 61 acaba de
  resolver para frontmatter.
- `graphify-out/manifest.json` sigue trackeado con rutas absolutas → regenerar con rutas
  relativas o untrackear.
- El frontmatter de `explore_workstation_ui_state_2.md` (el único NOTE restante): completarlo
  es 2 minutos, pero es reescribir un reporte histórico — misma conversación que la
  migración (b), decidir con el humano.

## 3. Cómo probar la skill con estas herramientas

**a) La sonda de instalación (ya corre, evidencia de hoy):**

```bash
bash tests/lab_skill_install.sh
# == 1. scaffold.sh ==            -> OK   (bash puro, única pieza ejecutable hoy)
# == 2. npm install ==            -> FAIL (EUNSUPPORTEDPROTOCOL workspace:*)
# == 3. build tsc -b ==           -> FAIL (sin typescript; tsconfig referencia ../packages)
# == 4. node dist/feature.js ==   -> FAIL (dist/ gitignoreado)
```

**b) La skill como la usa un agente** (en un repo de laboratorio, nunca sobre este):
`/handyman analyze` y `/handyman bootstrap local` funcionan hoy incluso post-install
(solo necesitan SKILL.md + assets + scaffold.sh). `run-feature` y `review` **no**: ordenan
`node dist/*.js`. Esa asimetría es exactamente lo que 64-65 arreglan — probar los modos
declarativos ya, los ejecutables después de 64.

**c) Los trigger-evals de la skill:** después de editar SKILL.md (feature 65),
`node handyman/dist/evals.js` valida que los triggers sigan matcheando
(`tests/test_evals.sh` es la suite que lo cubre en el gate).

## 4. Condensar el uso de estas herramientas como usuario

Hoy la superficie son **cuatro modos distintos**: 12 entrypoints `node handyman/dist/<verbo>.js`,
`./init.sh`, el panel en `:3210`, y la skill `/handyman`. La condensación no es un
proyecto aparte — cae gratis del camino ya decidido, en tres niveles:

| Nivel | Qué | Cuándo |
|---|---|---|
| 1 — un solo comando | Campo `bin` con **un** binario `handyman` que despacha subcomandos: `handyman feature add`, `handyman preflight`, `handyman review`. Los 12 archivos pasan a ser detalle interno. Dispatcher de ~30 líneas; las suites bash siguen apuntando a `dist/*.js` para no romper el oráculo | dentro de la feature 64 |
| 2 — mismo comando para humano y agente | SKILL.md ordena `npx handyman-harness ...`: lo que el agente escribe es lo que el humano copia. Elimina la clase entera de instrucciones rotas | feature 65 |
| 3 — el navegador | Acciones `start`/`unblock` desde el panel (§3.3 del handoff anterior, sigue vigente y no cruza la frontera "no es un runner") | después de 64-65 |

**Ya disponible hoy, sin trabajo:** `/handyman` como interfaz condensada del agente;
`feature.js next` y `log` para no editar `current.md` a mano; el textarea del panel para
acceptance (evita el quoting infernal de shell que motivó §3.5 del handoff anterior).

**Explícitamente NO (ponytail):** un TUI/wrapper nuevo por fuera de esto. La condensación
real viene de tener UN nombre (`handyman`) en el PATH y el panel para lo visual — otra
capa sería una tercera superficie que mantener.

## 5. Trampas vigentes

1. **Nueva, medida hoy:** `npx tsc` a secas instala `tsc@2.0.4` — un paquete deprecado
   que **no es TypeScript**. Usar `npm exec --no -- tsc -b` (falla honesto si no está)
   o el build vía pnpm del monorepo.
2. `mktemp -d` en macOS devuelve `/var/...` symlink → entry guards salen 0 mudos.
   `phys_tmp()` / `pwd -P` (handoff anterior §5.1, sigue vigente).
3. Las cuatro trampas del handoff anterior §5 siguen todas vigentes (fixtures con
   symlink, greps negativos vs prosa, `apps/web` no importa `handyman` estáticamente).
4. El `handyman/skills-lock.json` filtrado **se queda por ahora** (decisión del humano
   2026-07-19); si molesta en el tarball de 64, es una línea del `files` whitelist.

## 6. Explícitamente NO

Se hereda completo el §6 del handoff anterior (MCP, relays como tools, `done` desde el
panel, endurecer `tools_discovery check`), y se suma: **no reinstalar la skill global
hasta 64-65** (§0) y **no construir wrappers de condensación fuera de los 3 niveles de §4**.
